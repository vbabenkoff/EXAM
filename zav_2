#include <stdio.h>
#include <stdlib.h>

int main ()
{
	int mas[50];
	int i, k, n, x;

	printf("\nx=");
	scanf("%d",&x);

	for(i = 0, k = 1; k < n; i++, k = k * (2 * x + 1)){
		mas[i] = k;
		printf("%d\n", mas[i]);
	}
	
	return 0;
}
